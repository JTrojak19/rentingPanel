{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tennant creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_widget(form.name, {'attr': {'placeholder': 'Last Name'} }) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('tennant_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
